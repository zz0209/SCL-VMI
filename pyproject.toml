[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sclvmi"
version = "0.1.0"
requires-python = ">=3.12"
description = "Reproducible volumetric medical imaging research pipelines"

[tool.setuptools.packages.find]
where = ["src"]
